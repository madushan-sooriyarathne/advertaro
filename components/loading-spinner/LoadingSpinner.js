import { OuterCircle, InnerCircle } from "./LoadingSpinnerStyles";

const LoadingSpinner = () => {
  return (
    <OuterCircle>
      <InnerCircle />
    </OuterCircle>
  );
};

export default LoadingSpinner;
